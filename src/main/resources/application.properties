spring.application.name=api

# MVC 뷰 설정
spring.mvc.view.prefix=classpath:/templates/
spring.mvc.view.suffix=.html
spring.web.resources.static-locations=classpath:/static/
# 스프링인데 mvc로 쪼개고, view에만 이 설정을 적용한다.
# 시작은 /이고 끝은 .html이다.
